<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StayNow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary_variant</item>
        <item name="colorOnPrimary">@color/my_light_on_primary</item>

        <!-- Customize button colors -->
        <item name="colorSecondary">@color/my_light_secondary</item>
        <item name="colorOnSecondary">@color/my_light_on_secondary</item>
        <item name="buttonStyle">@style/CustomButton</item>
    </style>

    <style name="Theme.StayNow" parent="Base.Theme.StayNow" />

    <!--    Bo goc Floating Action Button-->
    <style name="FABCornerShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50dp</item> <!-- Điều chỉnh kích thước bo viền -->
    </style>

    <style name="CustomButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/my_button_background</item>
        <item name="android:textColor">@color/my_button_text</item>
    </style>

    <!-- Style cho Custom Dialog -->
    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowMinWidthMajor">90%</item> <!-- Đặt chiều rộng dialog -->
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:windowBackground">@drawable/dialog_background</item> <!-- Nền custom -->
        <item name="android:backgroundDimEnabled">true</item> <!-- Làm mờ nền phía sau -->
        <item name="android:textColor">@color/black</item> <!-- Màu chữ mặc định -->
    </style>

<!--    Border cho Image trong RecyclerView chi tiet tro-->
    <style name="BorderImageItem">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
<!--        <item name="strokeColor">@color/purple_border_bottom</item>-->
<!--        <item name="strokeWidth">8dp</item>-->
    </style>

<!--    Border anh thanh hinh tron-->
    <style name="CircularShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>
